// Legal & Compliance Links Configuration
// Update these URLs to point to your actual legal pages

const LEGAL_LINKS = {
    privacyPolicy: '/privacy-policy.html',
    termsOfService: '/terms-of-service.html',
    supportEmail: 'support@flipforge.ai',
    legalEmail: 'legal@flipforge.ai',
    privacyEmail: 'privacy@flipforge.ai'
};

// Export for use in other files
if (typeof module !== 'undefined' && module.exports) {
    module.exports = LEGAL_LINKS;
}
