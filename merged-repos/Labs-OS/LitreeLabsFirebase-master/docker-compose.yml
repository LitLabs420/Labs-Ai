version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: litree-labstudio-app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      # Add your environment variables here
      # NEXT_PUBLIC_FIREBASE_API_KEY: ${NEXT_PUBLIC_FIREBASE_API_KEY}
      # NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
      # NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${NEXT_PUBLIC_FIREBASE_PROJECT_ID}
      # Add more as needed from your .env.production
    volumes:
      - ./public:/app/public:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - litree-network

  # Optional: Firebase Emulator for development/testing
  # firebase-emulator:
  #   image: node:20-alpine
  #   container_name: firebase-emulator
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #   ports:
  #     - "4000:4000"
  #     - "8080:8080"
  #   command: npx firebase emulators:start
  #   networks:
  #     - litree-network

networks:
  litree-network:
    driver: bridge

volumes:
  # Add any named volumes if needed
  # data:
  # cache:
